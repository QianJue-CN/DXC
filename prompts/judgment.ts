export const P_JUDGMENT_EASY = `【判定系统：轻松 (Easy)】
你必须在 \`shortTerm\` 或剧情文本中写出判定行，格式如下：
\`\`\`text
〔行动:结果, 判定值:158/难度:120, 基础:130, 幸运:+10, 环境:+18, 状态:-0〕
\`\`\`

【核心公式】
最终判定值 = (基础能力 + 等级补正) + 幸运 + 环境修正 + 状态修正

【轻松难度修正】
1) 基础难度整体 -80。
2) 跨级挑战惩罚降低：
   - 高1级: +300
   - 高2级: +700
3) 结果表宽容：
   - 差值 < -150 才为大失败。
   - 差值 >= 0 即为成功。
4) 允许一次“绝境回避”叙事描写（非数值回滚）。`;

export const P_JUDGMENT_NORMAL = `【判定系统：普通 (Normal)】
你必须在 \`shortTerm\` 或剧情文本中写出判定行，格式如下：
\`\`\`text
〔行动:结果, 判定值:158/难度:120, 基础:130, 幸运:+10, 环境:+18, 状态:-0〕
\`\`\`

【核心公式】
最终判定值 = (基础能力 + 等级补正) + 幸运 + 环境修正 + 状态修正

【普通难度阈值】
- 简单(杂兵): 判定值 > 属性平均值 - 100
- 普通(精英): 判定值 > 属性平均值
- 困难(楼层主): 判定值 > 属性平均值 + 200
- 高1级: 难度基准 + 500
- 高2级: 难度基准 + 1000

【结果表】
- 差值 < -200: 大失败
- 差值 < 0: 失败
- 差值 >= 0: 成功
- 差值 >= 150: 大成功
- 差值 >= 300: 完美`;

export const P_JUDGMENT_HARD = `【判定系统：困难 (Hard)】
你必须在 \`shortTerm\` 或剧情文本中写出判定行，格式如下：
\`\`\`text
〔行动:结果, 判定值:158/难度:120, 基础:130, 幸运:+10, 环境:+18, 状态:-0〕
\`\`\`

【核心公式】
最终判定值 = (基础能力 + 等级补正) + 幸运 + 环境修正 + 状态修正

【困难难度修正】
1) 基础难度整体 +120。
2) 跨级挑战惩罚提高：
   - 高1级: +700
   - 高2级: +1300
3) 结果表更残酷：
   - 差值 < -120 即为大失败。
   - 差值 < 20 视为失败（需要明显优势才成功）。
4) 叙事中强调体力消耗、受伤后续影响。`;

export const P_JUDGMENT_HELL = `【判定系统：地狱 (Hell)】
你必须在 \`shortTerm\` 或剧情文本中写出判定行，格式如下：
\`\`\`text
〔行动:结果, 判定值:158/难度:120, 基础:130, 幸运:+10, 环境:+18, 状态:-0〕
\`\`\`

【核心公式】
最终判定值 = (基础能力 + 等级补正) + 幸运 + 环境修正 + 状态修正

【地狱难度修正】
1) 基础难度整体 +200。
2) 跨级挑战惩罚极端：
   - 高1级: +900
   - 高2级: +1600 (几乎必败)
3) 成功门槛提高：
   - 差值 < 50 视为失败。
   - 差值 < -80 即为大失败。
4) 无“保底”，失败必须产生真实代价。`;
